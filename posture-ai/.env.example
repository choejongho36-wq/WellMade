# Anthropic API 키 (LLM 코멘트 생성용).
# 없어도 서버는 정상 동작한다 — 코멘트만 결정론적 템플릿 문구로 대체된다
# (comment.source == "fallback", reason == "no_api_key").
ANTHROPIC_API_KEY=

# 사용할 모델 (선택). 기본값: claude-haiku-4-5-20251001
# 작은 JSON을 받아 2~3문장을 쓰는 가벼운 작업이라 상위 모델이 필요 없다.
# POSTURE_LLM_MODEL=claude-haiku-4-5-20251001
