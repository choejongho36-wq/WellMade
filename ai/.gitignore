__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# 실제 API 키 등 비밀값 — 예시 파일(.env.example)만 커밋한다.
.env

# ML 학습 원본 데이터 (용량 문제로 커밋 안 함 — 재현 방법은 ml_training/README.md 참고)
ml_training/data/

# (2026-08-28 추가) S3에서 받아온 DTW 템플릿 묶음의 로컬 캐시 —
# app/pose/dtw_template_store.py 참고. 배포 산출물이라 커밋 대상 아님.
.cache/
