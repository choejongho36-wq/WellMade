"""
스쿼트 자세 판정에 쓰는 공통 규칙(정상범위 · 임계값 · 개인화 계산).

NORMAL_RANGES/각종 임계값(threshold)/personalized_hip_range()는 실시간 코칭(AI-06,
coaching/realtime.py의 judge_realtime_coaching())이 가져다 쓴다.

"파인튜닝 금지, 규칙기반 우선" 원칙에 따라, 각도 계산 결과를 사전에 정의한 정상 범위
(NORMAL_RANGES)와 비교하는 방식으로 구현했다. 규칙기반을 택한 이유:
1) 스쿼트의 "바른 자세" 각도 기준은 운동/재활 자료에 이미 잘 정리되어 있어, 별도 라벨링
   데이터 없이도 신뢰할 수 있는 규칙을 세울 수 있다.
2) 규칙기반은 판정 근거를 사람이 바로 설명할 수 있어(설명가능성), "왜 이상 자세로
   판정했는지"를 코칭 문구·RAG 검색 쿼리에 그대로 재사용하기 좋다.
"""

from app.schemas import HipFlexibilityCalibration

# 스포츠의학/트레이너 자격 기준 자료를 근거로 한 값.
#
# 주의: 아래 문헌들이 말하는 "무릎 굴곡각(knee flexion angle)"은 다리를 편 상태를 0도로
# 두고 "얼마나 굽혔는지"를 재는 값이라, get_knee_angle()이 계산하는 "관절 내각"(편 상태가
# 180도)과는 서로 보각(180 - 굴곡각) 관계다. 아래 범위는 전부 내각 기준으로 이미 변환해뒀다.
#
# IJSPT(International Journal of Sports Physical Therapy)의 스쿼트 임상 리뷰 논문은 깊이를
# 얕음(굴곡 0~90도) / 평행(90~110도) / 깊음(110~135도)으로 나눈다. 내각 기준 하한/상한(아래
# 30~120도)은 실측 오탐 사례들을 반영해 넓게 잡혀 있다 — 극단적인 이상(전혀 안 앉음 / 무릎에
# 무리가 갈 정도로 과도하게 접음)만 잡고, 정상 범위 안의 깊이 편차는 폭넓게 허용한다.
# 참고: https://ijspt.scholasticahq.com/article/94600-a-biomechanical-review-of-the-squat-exercise-implications-for-clinical-practice
# TODO: 팀 확정 필요 — 30/120은 문헌 기준이 아니라 실측 오탐 방지용 잠정치. 범위가 너무 넓어
# 실제 이상 자세를 놓치는 건 아닌지 실사용자 테스트 후 재검토 필요.
#
# 같은 논문은 엉덩이(고관절) 각도에 대해서는 "개인의 고관절 가동범위가 달라 고정된 정상범위를
# 제시하기 어렵다"고 명시한다. 그래서 hip_angle 범위는 "이 범위를 벗어나면 상체를 과도하게
# 숙이거나 너무 꼿꼿이 세운 상태"라는 느슨한 안전장치로만 쓰고, "이 범위 안 = 완벽한 자세"로
# 해석하지 않는다.
NORMAL_RANGES = {
    "knee_angle": (30, 120),
    "hip_angle": (25, 120),
}

# 개인별 고관절 유연성 캘리브레이션(HipFlexibilityCalibration)이 있을 때, "그 사람이 낼 수
# 있는 최대 가동범위" 중 몇 %~몇 % 구간을 정상으로 볼지 정하는 값. 100%(자기 한계까지)로
# 잡지 않고 70~90%로 여유를 두는 이유는, 캘리브레이션 때 잰 "무리하지 않는 선의 최대치"
# 자체가 이미 안전 마진이 있는 값이라, 실제 운동 중 매 반복을 그 한계까지 밀어붙이면 부상
# 위험이 커지기 때문이다.
# TODO: 팀 확정 필요 — 사용자 테스트 후 조정.
CALIBRATION_LOW_PCT = 0.7
CALIBRATION_HIGH_PCT = 0.9

# 발뒤꿈치가 얼마나 뜨면 이상으로 볼지의 임계값. get_heel_lift_ratio()가 반환하는 값
# (발 길이 대비 발뒤꿈치-발끝 높이차 비율)과 비교한다.
# (2026-08-27 재조정) 원래 0.5는 실측 문헌값이 아니라 상식적으로 잡은 잠정치였는데,
# 실제 측면 촬영 "정상"(발뒤꿈치가 바닥에 붙은) 참고 사진 10장으로 이 비율을 실측해보니
# 0.013~0.474(평균 0.157) 사이로 나왔다 — 전부 양수 쪽으로 몰려있어(발끝 랜드마크가
# 원근상 발뒤꿈치보다 낮게 찍히는 구조적 편향으로 추정) 0.5 임곗값과 거의 붙어있는
# 사례(0.474, 0.335)가 있었다. 즉 정상 자세인데도 임곗값을 살짝만 넘기면 오탐이 나는
# 구조라, 관측된 정상 최댓값(0.474)에 방어적 여유를 크게 두고 0.7로 올렸다.
# **중요한 한계**: 이 값도 DTW_NEAREST_DISTANCE_THRESHOLD와 동일하게 "정상 대 정상"
# 비교로만 잡은 잠정치다 — 실제로 발뒤꿈치가 뜬(이상) 사진 데이터는 하나도 없어서, 진짜
# 정상/이상 경계를 실측한 게 아니다. 표본도 10장뿐이라 좁다.
#
# (2026-08-27 추가) 이 값은 두 가지 용도로 재사용된다.
# 1) angle_history 안에서 "이번 렙이 시작되기 직전 서 있던 프레임"을 못 찾을 때(예:
#    히스토리가 이미 깊게 앉은 채로 시작하는 등) — heel_lift_ratio 절대값과 그대로
#    비교한다(기존 방식, 위 실측 근거 그대로 적용).
# 2) 그 "직전 서 있던 프레임"을 찾았을 때 — 절대값이 아니라 "그때보다 지금 얼마나 더
#    들렸는지"(차이값)와 비교한다. 웨지·역도화처럼 원래 발뒤꿈치를 들고 서는 사용자도
#    "서 있는 프레임" 자체가 이미 그 상태로 찍히므로(realtime.py의
#    _find_standing_baseline_before_dip 참고), 차이값 기준으로 바꾸면 "동작 중에 실제로
#    더 들렸는지"만 잡을 수 있다.
#    (2026-08-27, 하루 안에 재정정) 원래는 온보딩 캘리브레이션(hip_calibration.
#    standing_heel_lift_ratio, 세션 시작 전 1회 측정)으로 이 기준값을 얻으려 했는데,
#    온보딩은 보통 "힐업 안 한" 평상시 자세로 측정한다 — 웨지 사용 여부는 그날그날
#    세션에서 정해지는 선택이라 온보딩 시점엔 반영이 안 된다는 지적이 나와, 매 렙마다
#    실시간으로 "방금 서 있던 상태"를 그대로 기준값으로 쓰는 방식으로 바꿨다. 사진
#    판정처럼 "직전 서 있는 프레임"이 아예 없는 입력(스쿼트 중인 사진 한 장뿐)은
#    여전히 위 1)번 절대값 경로를 탄다.
# 두 용도에 같은 상수를 재사용하는 건 정확한 근거보다는 편의상 선택이다 — 차이값 기준으로는
# 아직 실측 데이터(같은 사람의 "서 있을 때 vs 스쿼트 중" 페어)가 전혀 없다.
# TODO: 팀 확정 필요 — 더 많은 정상 사진 + 실제 "발뒤꿈치 뜸" 사진으로 절대값 재검증,
# 웨지 사용자 페어 데이터로 차이값 임곗값 별도 검증.
HEEL_LIFT_RATIO_THRESHOLD = 0.7

# 무릎 사이 너비가 발목 사이 너비의 몇 %보다 좁아지면 무릎 모임(valgus)으로 볼지의 잠정
# 임계값. get_knee_valgus_ratio()가 반환하는 값(무릎너비/발목너비)과 비교한다. 0.8은 "무릎이
# 발목 너비의 80% 미만으로 좁아지면 눈에 띄게 모인 것"이라는 상식적인 잠정치다.
# TODO: 팀 확정 필요 — 실사용자 테스트로 조정.
KNEE_VALGUS_RATIO_THRESHOLD = 0.8

# (2026-08-27 폐기) 원래 여기 "고관절 과신전(hip hyperextension) 의심" 신호로
# HIP_HYPEREXTENSION_VALGUS_THRESHOLD(1.1)를 두고, get_knee_valgus_ratio()가 0.8~1.1
# 사이면 별도 태깅했었다. N=1 실측 사례 하나로 잡은 잠정치였는데, 이번 세션에서 두 가지
# 근거로 폐기했다:
# 1) 실제 정면 사진(무릎이 발목보다 넓게 벌어진, 누가 봐도 정상인 스쿼트) 검증에서
#    knee_valgus_ratio=1.064가 이 구간에 걸려 "고관절 과신전 의심"으로 오탐하는 걸 확인.
# 2) 생체역학 논문("How to squat? Effects of various stance widths, foot placement
#    angles and level of experience on knee, hip and trunk motion and loading", BMC
#    Sports Science 2018)의 ΔD*(무릎의 시상면 대비 정면 변위, 다리 길이로 정규화 —
#    음수=valgus/안쪽 모임, 양수=varus/바깥쪽 벌어짐) 실측 그래프를 보면, 정상/권장
#    스탠스 조건들도 스쿼트가 깊어질수록 varus 방향(양수)으로 다리 길이의 15~17%까지
#    커진다 — 즉 무릎이 발목보다 넓게 벌어지는 것 자체는 딥스쿼트에서 흔하고 안전한
#    패턴이며, 이 논문이 실제로 경계하는 건 오직 valgus(음수, 안쪽 모임) 방향뿐이다.
#    "무릎-발목이 일직선이어야 최적"이라는 주장도 이 논문의 실제 결론이 아니었다(극단적인
#    스탠스/발각도 조합만 피하라는 게 논문의 결론).
# 결론: get_knee_valgus_ratio()가 KNEE_VALGUS_RATIO_THRESHOLD(0.8, valgus 방향) 미만일
# 때만 위험 신호로 보고, 그 이상(varus 방향, 심지어 1.1을 넘는 값도)은 이 지표만으로는
# 별도 위험 신호로 취급하지 않는다. "진짜 고관절 과신전"을 감지하려면 애초에 무릎 너비가
# 아닌 다른 지표(예: 골반/요추 정렬)가 필요하다 — 아직 그런 지표는 없다.

# 무릎이 발끝보다 얼마나(허벅지 길이 대비 비율) 더 나가면 "발끝을 넘었다"고 볼지의
# 잠정 임계값. get_knee_over_toe_ratio()가 반환하는 값(무릎-발끝 거리 / 엉덩이-무릎
# 허벅지 길이)과 비교한다.
# (2026-08-27 변경) 예전에는 발 길이 정규화 없는 원시 좌표 거리(이미지 너비 대비 비율)를
# 기준으로 0.03을 썼는데, 발이 스탠스 때문에 바깥으로 돌아가면(외회전) 발 길이 자체가
# 줄어들어 자로 쓰기 불안정하다는 게 확인돼(정상적인 딥스쿼트 사진에서 오탐 발생,
# checklist 2026-08-27 addendum 참고) 허벅지(엉덩이-무릎) 길이 기준으로 바꿨다. 단위가
# 완전히 달라져 예전 0.03이라는 값 자체는 더 이상 의미가 없다 — 아래 값은 실측 검증 전
# 임시 추정치다.
# TODO: 팀 확정 필요(중요) — 이 값은 아직 실제 데이터로 보정하지 않았다. 발 길이 임곗값
# (0.03)은 실측 정상 사례 기준으로 잡았던 반면, 이건 build_dtw_templates.py 체크포인트에
# 무릎-발끝/허벅지 원시 좌표가 저장돼 있지 않아 이번 세션에서 곧바로 재계산하지 못했다.
# 실사용자 테스트 또는 기존 정상 영상 재추출(mediapipe 재실행 필요)로 반드시 재검증할 것.
KNEE_OVER_TOE_RATIO_THRESHOLD = 0.2

# 목/시선(고개가 앞으로 떨어졌는지) 판정 임계값. get_shoulder_forward_lean_deg()가 반환하는
# 값(목 기울기 - 상체 기울기, 도 단위)과 비교한다. 0 이하는 목이 상체와 비슷하거나 더
# 세워진 정상 자세이므로, 그보다 얼마나 더 커야 "고개가 앞으로 떨어졌다"고 볼지를 정하는
# 값이다.
# (원래는 "어깨 말림" 판정에도 이 값을 같이 썼으나, 어깨 말림은 원인을 "등이 굽었다"로
# 단정해 등 굽음 로직(아래 BACK_ROUNDING_RATIO_THRESHOLD)에 통합하고, 여기는 목/시선
# 전용 신호로 분리했다.)
# (2026-08-27 변경) 원래 20.0은 실측 정상 사례 딱 하나(-29.6도)로 잡은 값이었는데, 실제
# "정상"이라고 확인된 사진 11장을 추가로 실측해보니 값이 -29.7도~36.9도까지 걸쳐 있었다
# (checklist 2026-08-27 addendum 7번 참고). 특히 이 지표는 get_shoulder_forward_lean_deg가
# 어깨-귀라는 짧은 구간에 의존하는데, 귀 랜드마크가 정확히 귓불이 아니라 관자놀이 등
# 살짝 옆으로 찍히기만 해도(구간이 짧아 절대 픽셀 오차가 상대적으로 크게 각도에 반영됨)
# 십수 도 단위로 흔들리는 게 실측으로 확인됐다 — 그 노이즈 폭까지 감안해 여유를 늘렸다.
# 40.0도 여전히 실제 "나쁜 자세(고개가 진짜 떨어진 경우)" 상한을 실측한 적은 없는
# 잠정치다 — 지금까지 확인된 정상 범위(~37도)에 노이즈 여유를 더한 값일 뿐이다.
# TODO: 팀 확정 필요 — "진짜 나쁜 자세"쪽 실측 사례로 상한을 재검증할 것. 근본적으로는
# 귀 하나의 좌표에 의존하는 이 지표 자체가 노이즈에 취약하다는 문제도 남아있다.
SHOULDER_FORWARD_LEAN_THRESHOLD_DEG = 40.0

# 등 굽음(척추 굴곡) 판정 임계값. get_torso_length_ratio()가 반환하는, 어깨-엉덩이 직선거리를
# 발 길이로 정규화한 값과 비교한다. 사람마다 체형(어깨-엉덩이 길이 대 발 길이 비율)이 달라
# 고정된 절대 기준값을 둘 수 없으므로, 온보딩 캘리브레이션
# (HipFlexibilityCalibration.standing_shoulder_hip_ratio, "편하게 서 있을 때" 잰 이 사람의
# 기준 비율) 대비 "몇 % 이상 줄었는지"로 판정한다. 캘리브레이션이 없으면(하위 호환) 이 검사
# 자체를 건너뛴다. 0.85는 "기준값보다 15% 이상 줄어들면 등이 굽었다고 본다"는 뜻.
# TODO: 팀 확정 필요 — 실사용자 테스트로 15%라는 폭 자체를 재검토해야 한다.
BACK_ROUNDING_RATIO_THRESHOLD = 0.85

# 무게중심 관련(상체가 정강이보다 얼마나 더 앞으로 기울었는지) 판정 임계값.
# get_torso_shin_lean_gap_deg()가 반환하는 값(상체 절대 기울기 - 정강이 절대 기울기, 둘 다
# 수직선 기준)과 비교한다.
#
# 배경: 팀원이 "무게중심이 무너져서 앞에 반대 방향 무게(바/플레이트)가 없으면 뒤로 넘어질
# 것 같다"고 지적한 실제 사진 2장을 실측한 결과다. 처음엔 "엉덩이가 지지기반(발뒤꿈치)보다
# 얼마나 뒤로 벗어났는지"를 발 길이/허벅지 길이로 정규화해서 시도했는데, 정상 사진 10장 안에서도
# 편차가 너무 커서(0.1~1.2 사이) 구분이 안 됐다 — 예전에 비슷한 접근(엉덩이-어깨/무릎 직선
# 기준 이탈)을 실제 적용했더니 모든 사진에서 이상이 뜨는 문제로 폐기했던 것과 같은 실패
# 패턴이었다. 대신 상체·정강이 각각의 절대 기울기 "차이"를 보니, 절대 기울기 하나하나는
# 체형(다리 길이 등)에 따라 정상 사진 안에서도 넓게 퍼졌지만(예: 상체 기울기만 봐도
# 15.6~48.9도), 차이값은 정상 사진 10장 전부 -2.0~23.3도인데 지적받은 사진 2장은 각각
# 27.2도·28.9도로 정상군 범위 밖에 일관되게 모였다. 25.0은 이 둘 사이(정상 최댓값 23.3 /
# 나쁜 사례 최솟값 27.2)의 중간 지점이다.
# TODO: 팀 확정 필요(매우 중요) — 나쁜 사례가 단 2건뿐이라 임계값 검증 표본이 매우 작다.
# "무게중심이 무너졌다"고 확인된 사례를 더 모아서 반드시 재검증할 것. 근본적으로 이 지표는
# 2D 이미지 평면(x/y)만으로 계산되는 근사치라, 실제 무게중심(몸통+머리+팔+들고 있는 무게를
# 합친 것)이나 깊이 방향의 균형은 반영하지 못한다는 한계도 있다.
TORSO_SHIN_LEAN_GAP_THRESHOLD_DEG = 25.0


# (2026-08-27 추가) DTW(동적 시간 워핑) 기반 "정상 렙 패턴과의 유사도" 판정 임곗값.
# 배경: claude/wellmade-squat-criteria-checklist.md 실행 대기 3번 — 허리 과신전/등 굽음은
# 정지 사진 한 장으로는 원리적으로 판정 불가능해(시계열이 있어야 DTW가 성립) 실시간
# 영상 경로에서 "정상 렙 20개(app/pose/dtw_templates/*.json)와의 패턴 거리"로 1차
# 판정하기로 확정했다. 원래 설계는 "거리가 애매한 구간만 LLM 2차 확인"하는 하이브리드였고,
# "애매한 구간"을 어떻게 정할지(정성/정량 곡선 이상치 탐지)가 checklist 2번 항목에 아직
# 🧪 미정으로 남아있다 — 이 상수는 그 설계가 확정되기 전, 사용자 지시("dtw 인프라
# 적용하라고 그냥 1개를 바탕으로 적용해 나중에 숫자만 바꾸면 되는거아니야?")에 따라
# LLM 하이브리드 없이 단순 스칼라 임곗값 하나로 우선 배선한 잠정치다.
#
# 값 산정 근거(2026-08-27 실측, ai/app/pose/dtw_templates/*.json 20개 기준):
# - 템플릿 20개 각각을 쿼리로 보고 자기 자신을 뺀 나머지 19개와 비교하는
#   leave-one-out 최근접거리를 실측했다.
# - 실제 렙 단위 템플릿(영상에서 무릎각도 150도 기준으로 잘라낸 렙 12개, 길이 58~117
#   프레임)만 보면 최근접거리가 3.80~9.11(평균 5.16)로 좁게 모였다 — 실시간 쿼리도
#   같은 방식(무릎 150도 기준)으로 잘라낸 렙 1개이므로 이 범위가 가장 직접적인 참고값.
# - 연속촬영 이미지 구간 템플릿 8개(길이 10~276, 렙 1개가 아니라 5~7초 간격의 연속
#   촬영 세션이라 훨씬 긺)는 최근접거리가 3.47~19.29까지 더 넓게 퍼졌다 — 다만 이건
#   비교 대상 시퀀스 길이 자체가 크게 다른 데서 오는 것으로 보인다(짧은 쿼리와 아주
#   긴 템플릿을 dtaidistance로 비교하면 워핑 비용이 커진다 — /ml-test에 추가하려던
#   6프레임 윈도우안이 정확히 이 문제로 기각된 것과 같은 종류의 편향, 다만 실시간
#   렙 쿼리는 6프레임이 아니라 렙 전체 길이라 이 편향은 훨씬 약하다).
# - **매우 중요한 한계**: 위 수치는 전부 "정상 대 정상" 비교에서 나온 값이다 — 실제
#   "나쁜 폼" 렙의 DTW 거리를 하나도 확보하지 못한 상태라, 이 임곗값은 정상 범위
#   관측값(최대 19.29)에 방어적 여유를 크게 둔 것일 뿐 정상/이상을 실제로 가르는
#   경계선을 실측한 게 아니다. checklist 2번 항목(애매한 구간 판별 방식)이 확정되고
#   실제 "나쁜 폼" 렙 데이터가 모이면 반드시 재검증해야 한다.
DTW_NEAREST_DISTANCE_THRESHOLD = 20.0

# (2026-08-28 추가) DTW+LLM 하이브리드의 "애매한 구간" 경계 —
# app/coaching/hyperextension_llm_check.py 참고. 최근접거리가 이 범위 안에 들어오면
# 바로 form_pattern으로 태깅하지 않고 LLM 2차 확인으로 넘긴다(그마저도 설정 안 돼
# 있으면 아래 하한/상한 근거에 적었듯 기존 임곗값 방식으로 되돌아간다 — 하위 호환).
# 근거: DTW_NEAREST_DISTANCE_THRESHOLD와 마찬가지로 "나쁜 폼" 렙 데이터가 없어
# 정성적으로 잡은 잠정 범위다.
#   - 하한(10.0, 임곗값의 절반): "정상 대 정상" leave-one-out 관측 최댓값(19.29,
#     바로 위 주석 참고)보다 한참 낮은 지점부터 "혹시 모르니 LLM에 물어보자"로
#     넉넉하게 잡았다.
#   - 상한(30.0, 임곗값의 1.5배): 실측으로 확인한 "심하게 왜곡된 렙"(hip_angle
#     +60도·shoulder_forward_lean_deg +80도·torso_length_ratio 0.3배 왜곡 — 거리
#     41.9, tests/test_api.py의 DTW 왜곡 테스트 참고)보다는 확실히 낮은 지점 — 이
#     정도로 벗어난 렙은 LLM 응답(20~40초)을 기다릴 것도 없이 바로 이상으로 본다.
DTW_AMBIGUOUS_LOWER_DISTANCE = 10.0
DTW_AMBIGUOUS_UPPER_DISTANCE = 30.0

# LLM 2차 확인 job 결과를 서버가 잠깐 담아두는 최대 시간(초) —
# app/coaching/hyperextension_llm_check.py의 job 저장소 TTL. 프론트가 이 시간 안에
# pending_llm_job_id를 다시 보내 결과를 받아가지 않으면(세션 종료, 네트워크 문제 등)
# 버린다. 실측 지연시간(20~40초)의 몇 배 여유를 둔 값.
LLM_HYPEREXTENSION_JOB_TTL_SECONDS = 300

# 실시간 히스토리에서 "렙 1개"를 잘라낼 때(무릎각도가 STANDING_KNEE_ANGLE_MIN 아래로
# 내려갔다 다시 올라오는 구간) 이보다 짧으면 렙이 아니라 노이즈(카메라 흔들림 등으로
# 순간적으로 각도가 걸린 경우)로 보고 DTW 비교 자체를 건너뛴다.
# ml_training/build_dtw_templates.py의 MIN_REP_FRAMES(템플릿을 만들 때 쓴 것과
# 동일한 기준, 5)을 그대로 따른다 — 쿼리와 템플릿이 서로 다른 최소 길이 기준으로
# 만들어지면 비교 자체가 일관성을 잃는다.
MIN_DTW_REP_FRAMES = 5


def personalized_hip_range(calibration: HipFlexibilityCalibration) -> tuple[float, float]:
    """
    캘리브레이션 결과(서 있을 때 각도, 무리 없이 최대한 숙였을 때 각도)로부터
    "이 사람 기준 정상 hip_angle 범위"를 계산한다.

    계산 방식은 "숙인 정도(가동범위)의 70~90% 지점을 정상 구간으로 본다": 자기 최대치의
    90%보다 더 깊이 숙이면 한계에 너무 가까워 위험 신호로, 70%보다 덜 숙이면 아직 목표한
    만큼 자세를 안 잡은 것으로 판정하겠다는 뜻이다.
    """
    available_range = calibration.standing_hip_angle - calibration.max_flex_hip_angle
    # 가동범위가 0 이하(측정값이 잘못 들어온 경우)면 계산이 무의미하므로, 계산을 포기하고
    # 서 있는 각도 자체를 상하한으로 반환해 "항상 범위 밖"으로 처리되게 한다 — 잘못된
    # 캘리브레이션 데이터로 엉뚱하게 "정상"이라고 판정하는 것보다는 안전한 실패 방식이다.
    if available_range <= 0:
        return calibration.standing_hip_angle, calibration.standing_hip_angle

    low = calibration.standing_hip_angle - CALIBRATION_HIGH_PCT * available_range
    high = calibration.standing_hip_angle - CALIBRATION_LOW_PCT * available_range
    return low, high
