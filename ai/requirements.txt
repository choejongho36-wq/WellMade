# WellMade AI 서버 의존성.
# 기술 원칙: 파인튜닝 금지 + 무거운 연산(자세추정)은 클라이언트에서 실행.
# 이 서버는 좌표/각도 등 경량 수치 데이터만 받아 규칙기반 판정 + LLM API 호출을 수행하므로,
# 딥러닝 프레임워크(torch, tensorflow 등)는 의도적으로 포함하지 않는다.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
python-multipart>=0.0.9

# --- 테스트용 (tests/test_api.py 실행에 필요) ---
pytest>=8.0
httpx>=0.27              # FastAPI TestClient가 내부적으로 사용

# --- 전통 ML 스쿼트/런지 분류기(app/ml/*_classifier.py, joblib>=1.3, numpy>=1.26 포함)는
# 2026-08-21 삭제됐다 — 실제 사진 테스트에서 신뢰도 문제가 확인돼, 카메라 전제를 "측면
# 단독"에서 "측면+정면 듀얼"로 바꾸며 규칙기반 판정으로 완전히 대체했다(자세한 배경은
# claude/wellmade-ai-progress.md 참고). scikit-learn은 아래 RAG 검색(TF-IDF)에 여전히
# 쓰이므로 남겨뒀지만, 더 이상 joblib으로 모델을 저장/로딩하지 않으므로(런타임에 메모리
# 내에서만 벡터라이저를 만들고 캐싱함 — retrieval.py의 _build_index() 참고) 예전처럼 버전을
# 엄격히 고정(==)해야 할 이유가 없어져 범위(>=)로 완화했다.
scikit-learn>=1.4
pandas>=2.2   # prepare_posture_reference.py에서 CSV 로딩에 사용

# --- DTW 궤적 유사도 매칭 (app/pose/dtw_matching.py) ---
# 허리 과신전/등 굽음을 실시간 영상에서 "정상 스쿼트 궤적과의 패턴 유사도"로 판정하기 위한
# 다변량 DTW 거리 계산에 쓴다(체크리스트 "척추/허리" v3, 2026-08-27 addendum 참고).
# fastdtw(근사)는 애매한 구간을 늘려 LLM 호출 비용을 오히려 키울 수 있어 제외, scipy
# 직접구현은 정확하지만 구현/테스트 부담 때문에 제외하고 dtaidistance(정식 DTW, C 구현)로
# 확정했다. mediapipe/opencv 같은 자세추정 라이브러리는 추가하지 않는다 — 좌표 추출·템플릿
# 생성은 로컬 오프라인 스크립트에서만 하고(서버는 계산이 아니라 저장·판정만 한다는 원칙),
# 이 라이브러리는 이미 만들어진 템플릿(숫자 배열)과의 거리 계산만 수행한다.
# 설치에는 C 컴파일러가 필요하다(OpenMP 가속 확장 포함) — macOS는 Xcode Command Line
# Tools, Linux는 build-essential 등이 이미 있으면 추가 설치 없이 빌드된다.
dtaidistance>=2.3,<3.0

# --- 하네스 오케스트레이션 (AI-07, app/orchestration/harness.py) ---
# scikit-learn과 같은 이유로 버전을 고정한다 — SDK의 tool_use 응답 형태가 버전마다
# 바뀔 수 있어 파싱 로직이 이 버전 기준으로 작성됐다. 실행에는 ANTHROPIC_API_KEY와
# HARNESS_LLM_MODEL 환경변수가 필요하다(둘 중 하나라도 없으면 규칙기반 폴백으로 동작 —
# harness.py의 _get_client()/decide_next_action() 참고).
anthropic==1.0.0

# --- RAG 검색 (AI-08/09/14, app/rag/retrieval.py) ---
# 요구사항 정의서는 벡터DB(Chroma)+임베딩 모델 조합을 제안하지만, 이 프로젝트는 대신
# scikit-learn TF-IDF(문자 n-gram)+코사인 유사도를 채택했다. chromadb/sentence-transformers를
# 새로 추가하지 않는 이유(비용, 딥러닝 프레임워크 배제 원칙, 기존 scikit-learn 재사용)는
# app/rag/retrieval.py 모듈 docstring에 자세히 적어뒀다 — scikit-learn은 이미 위에서
# 의존성에 포함돼 있어 이 섹션에 새로 추가할 패키지는 없다.
# TODO: 팀 확정 필요 — 검색 품질이 부족하다고 판단되면 그때 chromadb/sentence-transformers
# 도입을 재검토한다(retrieval.py의 search() 인터페이스는 그대로 유지한 채 내부만 교체 가능).
