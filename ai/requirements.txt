# WellMade AI 서버 의존성.
# 기술 원칙: 파인튜닝 금지 + 무거운 연산(자세추정)은 클라이언트에서 실행.
# 이 서버는 좌표/각도 등 경량 수치 데이터만 받아 규칙기반 판정 + LLM API 호출을 수행하므로,
# 딥러닝 프레임워크(torch, tensorflow 등)는 의도적으로 포함하지 않는다.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
python-multipart>=0.0.9

# --- 테스트용 (tests/test_api.py 실행에 필요) ---
pytest>=8.0
httpx>=0.27              # FastAPI TestClient가 내부적으로 사용

# --- 아래는 다음 단계(RAG 파이프라인, AI-08/09/14)에서 실제로 사용할 때 주석 해제 ---
# anthropic>=0.34          # LLM API (하네스 오케스트레이션, RAG 생성, 리포트 문구 생성)
# chromadb>=0.5             # RAG 벡터DB
# sentence-transformers>=3.0  # RAG 임베딩 (또는 Anthropic/OpenAI 임베딩 API로 대체 가능)
