# WellMade AI 서버 의존성.
# 기술 원칙: 파인튜닝 금지 + 무거운 연산(자세추정)은 클라이언트에서 실행.
# 이 서버는 좌표/각도 등 경량 수치 데이터만 받아 규칙기반 판정 + LLM API 호출을 수행하므로,
# 딥러닝 프레임워크(torch, tensorflow 등)는 의도적으로 포함하지 않는다.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
python-multipart>=0.0.9
python-dotenv>=1.0            # .env 파일을 읽어 os.environ에 주입 (app/main.py에서 서버 시작 시 로드)

# --- 테스트용 (tests/test_api.py 실행에 필요) ---
pytest>=8.0
httpx>=0.27              # FastAPI TestClient가 내부적으로 사용

# --- 전통 ML 스쿼트/런지 분류기(app/ml/*_classifier.py, joblib>=1.3, numpy>=1.26 포함)는
# 2026-08-21 삭제됐다 — 실제 사진 테스트에서 신뢰도 문제가 확인돼, 카메라 전제를 "측면
# 단독"에서 "측면+정면 듀얼"로 바꾸며 규칙기반 판정으로 완전히 대체했다(자세한 배경은
# claude/wellmade-ai-progress.md 참고). scikit-learn은 RAG 검색(TF-IDF)에만 쓰였는데
# RAG 자체가 2026-09-02에 이 스코프에서 완전히 삭제되며 함께 제거했다(joblib으로 모델을
# 저장/로딩하지도 않아 다른 용도로도 안 쓰인다 — sklearn 관련 코드는 더 이상 없음).
pandas>=2.2   # prepare_posture_reference.py에서 CSV 로딩에 사용
openpyxl>=3.1 # prepare_nutrition_reference.py에서 국민건강통계 엑셀 로딩에 사용
# 위 둘은 ml_training/의 일회성 전처리 스크립트 전용이다 - 서버 런타임(app/)은 미리 만들어둔
# JSON만 읽으므로(표준 라이브러리) 이 패키지들이 없어도 API는 정상 동작한다.

# --- DTW 궤적 유사도 매칭 (app/pose/dtw_matching.py) ---
# 허리 과신전/등 굽음을 실시간 영상에서 "정상 스쿼트 궤적과의 패턴 유사도"로 판정하기 위한
# 다변량 DTW 거리 계산에 쓴다(체크리스트 "척추/허리" v3, 2026-08-27 addendum 참고).
# fastdtw(근사)는 애매한 구간을 늘려 LLM 호출 비용을 오히려 키울 수 있어 제외, scipy
# 직접구현은 정확하지만 구현/테스트 부담 때문에 제외하고 dtaidistance(정식 DTW, C 구현)로
# 확정했다. mediapipe/opencv 같은 자세추정 라이브러리는 추가하지 않는다 — 좌표 추출·템플릿
# 생성은 로컬 오프라인 스크립트에서만 하고(서버는 계산이 아니라 저장·판정만 한다는 원칙),
# 이 라이브러리는 이미 만들어진 템플릿(숫자 배열)과의 거리 계산만 수행한다.
# 설치에는 C 컴파일러가 필요하다(OpenMP 가속 확장 포함) — macOS는 Xcode Command Line
# Tools, Linux는 build-essential 등이 이미 있으면 추가 설치 없이 빌드된다.
dtaidistance>=2.3,<3.0

# --- LLM 호출 전체 (AI-12 세션 리포트/고관절 과신전 LLM 2차 확인/"6랩 블라인드
# 테스트" 다중 벤더 비교 도구) — 2026-08-31부터 하네스(AI-07)는 완전 규칙기반으로
# 바뀌어 LLM을 호출하지 않고, RAG 생성(AI-09/14)은 2026-09-02에 이 스코프에서
# 삭제됨. ---
# 모든 LLM 호출은 boto3 bedrock-runtime 클라이언트의 Converse API(converse())를 직접
# 쓴다 — 벤더 전용 SDK(Anthropic SDK 등)를 쓰면 그 벤더 모델만 호출할 수 있는데, Converse
# API는 Anthropic/Amazon Nova/Meta Llama/Mistral 등 여러 벤더 모델에서 동일한 방식으로
# 도구 사용(toolConfig)을 지원하는 통합 인터페이스라, 모델을 바꿀 때 코드가 아니라
# 환경변수(각 모듈의 BEDROCK_MODEL_ID 계열 환경변수)만 바꾸면 된다.
# 실행에는 AWS_BEDROCK_REGION과 모듈별 모델 ID 환경변수(SESSION_REPORT_BEDROCK_MODEL_ID
# — session/report.py, HYPEREXTENSION_BEDROCK_MODEL_ID — hyperextension_llm_check.py),
# 그리고 AWS 자격증명(boto3 기본 체인 — 환경변수/프로파일/IAM 롤 중 하나)이 필요하다 —
# 없으면 모듈별 규칙기반 폴백으로 동작한다(각 모듈의 _get_client() 참고).
#
# (2026-08-28 추가) 같은 boto3를 app/pose/dtw_template_store.py도 재사용한다 — DTW
# 템플릿 묶음을 S3에서 받아올 때 쓰는 별도 기능(DTW_TEMPLATES_S3_* 환경변수)으로, 위
# Bedrock 용도와는 다른 AWS 리소스(S3 버킷)를 가리킨다. 이쪽도 환경변수가 없으면 조용히
# 로컬 폴백(dtw_template_store.py 모듈 docstring 참고).
boto3>=1.34
